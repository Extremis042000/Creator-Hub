# API Docs

No static API reference is maintained here — the backend generates a live, always-accurate one via springdoc-openapi:

- Swagger UI: `{backend URL}/swagger-ui.html`
- Raw OpenAPI spec: `{backend URL}/v3/api-docs`

Locally, that's `http://localhost:8080/swagger-ui.html`.
