# EXTREMIS Creator Hub — Development Roadmap

Status legend: ✅ done · ⏳ next · — not started

| Phase | Task | Depends on | Complexity | Completion criteria | Status |
|---|---|---|---|---|---|
| 0 | Business + technical blueprint | — | Low | This document set approved by founder | ✅ |
| 1 | PRD, personas, journeys, acceptance criteria | Phase 0 | Low | Written PRD covering all MVP tools | ✅ |
| 2 | System design: architecture, DB schema (JPA entities + Flyway), REST API contracts (OpenAPI), auth/payment/delivery flow diagrams | Phase 1 | Medium | Schema + diagrams reviewed, Flyway migrations drafted, OpenAPI spec drafted | ✅ |
| 3 | UI/UX: homepage, tools directory, tool page template, dashboard, login, pricing, store, checkout success, admin shell | Phase 2 | Medium | Clickable design covering all listed screens | ✅ |
| 4 | Project init — two repos/modules: `frontend` (Next.js 16/TS/Tailwind 4) and `backend` (Spring Boot 4/Maven, layered controller/service/repository) | Phase 2 | Low | Both boot locally; frontend can call a backend health endpoint | ✅ |
| 5 | Database + Flyway migrations live on free Postgres | Phase 4 | Low | Schema deployed via Flyway, seed data loads | ✅ |
| 6 | Shared frontend UI kit (buttons, inputs, cards, layout shell) + shared backend conventions (base Controller/Service patterns, DTO mapping, global exception handler) | Phase 4 | Low | Used consistently across first tool's frontend page and backend endpoint | ✅ |
| 7 | Homepage | Phase 6 | Low | Live, responsive, links to tools directory | ✅ |
| 8 | Tools directory page | Phase 6 | Low | Lists all 5 MVP tools with SEO copy | ✅ |
| 9 | KD Ratio Calculator (Spring Boot endpoint + Next.js page) | Phase 6 | Low | Deterministic, unit-tested service layer, SEO page complete | ✅ |
| 10 | Valorant Sensitivity Converter (Spring Boot endpoint + Next.js page) | Phase 6 | Medium | Conversion table verified against known values, service-layer unit tests | ✅ |
| 11 | BGMI Sensitivity Helper (Spring Boot endpoint + Next.js page) | Phase 6 | Medium | Recommendation logic documented as non-guarantee | ✅ |
| 12 | YouTube Title Generator (Spring Boot template engine + Next.js page) | Phase 6 | Medium | No AI cost; template variety validated | ✅ |
| 13 | YouTube Description Generator (Spring Boot template engine + Next.js page) | Phase 6 | Medium | SEO/hashtag sections populate correctly | ✅ |
| 14 | SEO layer (sitemap, robots, JSON-LD, OG/Twitter meta) | Phases 9-13 | Medium | All 5 tool pages pass on-page SEO checklist | ✅ |
| 15 | Analytics wiring (GA4 event tracking from frontend) | Phase 14 | Low | Founder's own GA property receiving events | ✅ (infra ready; no-ops until founder sets a real GA4 ID) |
| 16 | Authentication: Google OAuth handled in Spring Security + JWT session issued to Next.js frontend | Phase 5 | Medium | Login/logout/account deletion working end-to-end across both apps | ✅ verified live end-to-end (real Google sign-in + DB-confirmed soft-delete) |
| 17 | Admin basics (tools/products/flags read+edit via Spring Boot admin endpoints, plus admin-grants-admin) | Phase 16 | Medium | Founder can manage content without code changes | ✅ verified live end-to-end by founder (products deferred to Phase 19 — table doesn't exist yet). Founder's own browser QA caught 2 bugs after first sign-in: (1) tool "Premium only" edits failed — CORS was missing PATCH, fixed; (2) feature flags had no visible effect on the site — enabled flags now render as "Coming soon" cards on /tools via a new public `GET /api/v1/feature-flags` endpoint. Both fixed and verified live (backend tests, direct DB checks, and rendered HTML). |
| 18 | Affiliate architecture + disclosure (backend-tracked clicks) | Phase 17 | Low | Founder can add/toggle affiliate links; disclosure renders; clicks logged server-side | ✅ verified live end-to-end. New `affiliate_product`/`affiliate_click` tables (V7 migration); admin CRUD (create + edit + active toggle, no delete — matches Tool's lifecycle pattern) at `/api/v1/admin/affiliate-products`; public listing at `GET /api/v1/affiliate` (never exposes the real affiliateUrl); every click goes through `GET /api/v1/affiliate/{id}/redirect`, which logs a row then 302s to the merchant URL. New public `/gear` page with a standard FTC-style disclosure banner. User attribution on clicks is best-effort only (a plain `<a href>` navigation never sends an Authorization header), so most clicks will show as anonymous — acceptable since the acceptance criteria only requires clicks be logged, not attributed. Founder has not yet added a real product (needs actual affiliate program enrollment first — see "Hot Game Deals" note below for the honest substitute built in the meantime). |
| — | Homepage promotion reorder + "Hot Game Deals" (founder-requested, not in original roadmap) | Phase 18 | — | Coming Soon / Premium tools appear above regular content; a real, dynamic, non-fabricated deals feed exists for engagement | ✅ verified live. Homepage/`\|/tools` now order content as: Hot Game Deals → Coming Soon → Premium tools → regular tools (`Array.prototype.sort` on `premiumOnly`, no DB change needed). New `GameDealService` (package `com.extremis.hub.gamedeals`) polls the free, public, no-key CheapShark API (`storeID=1` i.e. Steam-only) every 6 hours via `@Scheduled`, caches in-memory, exposes `GET /api/v1/game-deals`; every purchase link points at the real, official `store.steampowered.com` page (verified live, returns the correct discounted title). New public `/deals` page + homepage teaser (top 3). **Honesty note for the founder:** this is an engagement/SEO content feature, not an affiliate-commission source — there is no legitimate way to "auto-fetch a real personal affiliate URL" from open data; real commission-earning links require the founder's own enrollment in a specific program (e.g. Amazon Associates), then adding them manually via the Phase 18 admin panel above. |
| 19 | Digital product architecture + secure delivery (Spring Boot signed download URLs) | Phase 17 | Medium | End-to-end purchase→download flow works in test mode | ✅ verified live end-to-end. New `product_category`/`product`/`orders`/`order_item`/`payment`/`user_entitlement` tables (V8 migration; `orders` plural to avoid quoting the reserved word). Admin CRUD for products (create/edit/active-toggle, category auto-upserted by slug) at `/api/v1/admin/products`. No real PaymentProvider yet (Phase 20, gated on founder KYC) — per founder's explicit choice, ANY signed-in user can `POST /api/v1/products/{id}/test-purchase` for $0, which creates a real Order(PAID)+OrderItem+Payment(provider="test")+UserEntitlement. **Kill switch:** `TEST_PURCHASES_ENABLED` env var (default true) — must be set to `false` once Phase 20 ships a real provider, so this never coexists with real payments. Downloads are never a public path: `GET /api/v1/products/{id}/download` checks entitlement and returns a short-lived (5 min) HMAC-signed token; `GET /api/v1/downloads/{token}` verifies signature+expiry+path-containment and streams the file. New `/store` page (purchase flow) and a "My digital products" section on `/dashboard` (re-download anytime). One placeholder "TEST — Sample Overlay Pack" ($0) product added for verification — replace with real content/product before real launch. Verified live: real purchase → all 4 DB rows confirmed via psql → signed URL fetched → downloaded bytes byte-for-byte identical to the source file → tampered and garbage tokens both correctly rejected (404). |
| 20 | Payment abstraction (`PaymentProvider` interface in Spring Boot, provider selected + onboarded by founder first) | Founder onboarding | Medium | Test-mode purchase completes via verified webhook handled by Spring Boot | ⏳ everything buildable without a real provider is done and verified live: provider-agnostic `PaymentProvider` interface (`com.extremis.hub.payment`), `CheckoutService` (creates a PENDING Order + delegates to the provider), `POST /api/v1/checkout/sessions`, `POST /api/v1/webhooks/payment` (idempotent — a duplicate webhook delivery is a silent no-op, not a duplicate Payment row), all wired via `Optional<PaymentProvider>` so the app runs fine with zero beans registered. Verified live: `POST /api/v1/checkout/sessions` correctly returns 422 "Real payments aren't set up yet" with no provider configured; Phase 19's free test-mode purchase flow (ownership + download) confirmed still fully working alongside this. **Founder initially chose Cashfree, then switched to PhonePe on 2026-09-16** (₹0 setup/AMC, 0% UPI transaction fee, ~2% on cards — better fit for a high-UPI-volume Indian store) — the Cashfree implementation was fully removed (package, config, tests, env vars, all doc references) and replaced, not layered alongside it. Concrete `PhonePePaymentProvider` implemented (`com.extremis.hub.payment.phonepe`) against PhonePe's own published Standard Checkout v2 API docs (OAuth `client_credentials` token endpoint, `checkout/v2/pay` Create Payment endpoint, `Authorization: SHA256(username:password)` webhook scheme, `checkout.order.completed`/`checkout.order.failed` payload shapes) — verified correct via 7 unit tests (valid signature accepted, tampered/wrong-credentials/missing-header signatures all rejected, case-insensitive header lookup, malformed order id rejected, both completed and failed event mapping). `PaymentProviderConfig` registers the bean ONLY once all five of `PHONEPE_CLIENT_ID`/`PHONEPE_CLIENT_SECRET`/`PHONEPE_CLIENT_VERSION`/`PHONEPE_WEBHOOK_USERNAME`/`PHONEPE_WEBHOOK_PASSWORD` are non-blank (deliberately not `@ConditionalOnProperty`, same reasoning as before); verified live that leaving them blank still returns the same 422 "not set up yet" and leaves Phase 19's test-purchase flow untouched. **Two real simplifications over the Cashfree design:** no customer phone number is needed at order creation (removes the placeholder-phone hack Cashfree required), and PhonePe returns a direct hosted-checkout `redirectUrl` (no client-side JS SDK integration needed, unlike Cashfree's `paymentSessionId` flow). **One real new constraint:** Standard Checkout v2 is INR-only — `PhonePePaymentProvider` rejects a non-INR order with a clear error rather than silently sending a wrong amount; real `Product` rows need `currency = "INR"`. **Honestly unverified: the live HTTP calls to PhonePe itself** — no sandbox credentials exist yet (onboarding in progress), and the webhook can't reach localhost without a public tunnel (e.g. ngrok) for real testing. One documented ambiguity in PhonePe's own docs, not silently assumed: the webhook `Authorization` header's `SHA256(username:password)` isn't specified as hex vs. base64 — implemented as hex (PhonePe's own legacy X-VERIFY scheme uses hex, and it's the near-universal convention), flagged in code to double-check against a real webhook delivery once credentials exist. |
| 21 | Display advertising integration points (ad slots on frontend, gated behind AdSense approval) | Phase 3, founder AdSense approval | Low | Ad slots render only once approved; no impact on Core Web Vitals when absent | ✅ verified live, both paths. Entirely frontend-only per the original architectural directive (`docs/architecture/07-phase2-system-design.md` — "gated by a FeatureFlag row", no dedicated table). Two independent switches, both required: `NEXT_PUBLIC_ADSENSE_PUBLISHER_ID` env var (set once AdSense approves) and the `display-ads` feature flag (toggled from the existing admin panel — lets the founder pull ads instantly without a redeploy, e.g. for a policy issue). `<AdSlot>` placed at the bottom of all 5 tool pages + the `/tools` directory; `<AdSenseScript>` loads the AdSense script once, site-wide, in the root layout. Verified live: with either switch off, zero `adsbygoogle` DOM footprint (confirmed zero Core Web Vitals impact); with both temporarily enabled for testing, the real ad markup (correct publisher ID, slot ID, script tag) rendered correctly, then cleanly reverted. `data-ad-slot` values are placeholder strings — replace with real per-placement AdSense ad unit IDs once created in the AdSense dashboard. |
| 22 | Premium entitlements (Spring Boot feature-flag/entitlement service) | Phase 20 | Medium | Feature flags gate premium tools correctly | ✅ verified live end-to-end. Closed a real gap: `Tool.premiumOnly` was previously cosmetic-only (frontend lock icon), with zero server-side check — anyone could call a premium tool's API directly and bypass it entirely. New `PremiumAccessService` (`com.extremis.hub.premium`) enforces access server-side in all 5 tool controllers via `requireAccessIfPremium(ToolType, Authentication)`, throwing a real 401/403 before the tool logic ever runs. Reuses Phase 19's generic `UserEntitlement` table with a new `"premium-tools"` entitlement key and `source="admin-grant"` (no new migration needed). New admin endpoints `GET/POST /api/v1/admin/premium-grants` + `DELETE /api/v1/admin/premium-grants/{userId}` (grant/revoke/list by email, mirrors the existing admin-grant pattern) and an `AdminPremiumUsersPanel` in the admin UI. `CurrentUserResponse`/`/api/v1/auth/me` now returns `hasPremiumAccess`. Frontend: `ToolCard` and a new `PremiumToolGate` (wrapping each of the 5 tool pages' forms) both fail-safe to locked/blank until the signed-in user's entitlement is confirmed client-side — a non-entitled visitor navigating straight to a premium tool's URL never sees the interactive form in the server-rendered HTML, only a blank placeholder that resolves to a "Premium Tool" message. Verified live: non-entitled signed-in user got a real 403 from both a newly-marked-premium tool (`kd-calculator`, toggled for the test then reset) and the founder's own already-premium `gaming-description-generator`; granting premium via the new admin endpoint immediately flipped both to success; `/auth/me` correctly reflected `hasPremiumAccess`; test grant revoked and `kd-calculator` reset to its original state afterward — founder's own premium toggle on `gaming-description-generator` was left untouched throughout. |
| 23 | Deployment: Next.js → Vercel, Spring Boot → free-tier Java host (e.g. Render/Railway free tier), env templates, CI/CD, prod DB, domain | Phases 1-22 | Medium | Both production URLs live, migrations applied | ✅ pulled forward out of order — Cashfree's merchant onboarding required a real live website URL. Actual hosting differs from the original plan: **Vercel was dropped** (their Hobby/free tier's ToS explicitly bans any payment processing or ad monetization — verified directly against Vercel's own fair-use docs — which conflicts with this site's whole business model) in favor of **Cloudflare Workers** (via the official `@opennextjs/cloudflare` adapter) — free, commercial-use explicitly allowed, no card required. Backend deployed to **Render free tier** via Docker (Render has no native JVM runtime) — commercial-use allowed there too, with known free-tier caveats (cold start after 15 min idle, ephemeral disk — the latter doesn't affect Phase 19 today since the one test product file is committed to git and baked into the image, but will need Cloudflare R2 or similar before real purchasable product files are added). Live URLs: frontend `https://extremis-creator-hub.surya-chowdhury0412.workers.dev` (this is the URL given to Cashfree), backend `https://extremis-creator-hub-backend.onrender.com`. Verified end-to-end live: backend health check, frontend SSR rendering, and a real cross-service call (Hot Game Deals section rendering real data fetched from the deployed backend). CORS updated to allow the production frontend origin. No custom domain yet (optional per the founder action checklist — zero-cost `*.workers.dev` subdomain used instead). |
| 24 | Monitoring + launch checklist | Phase 23 | Low | Error tracking + uptime check in place for both apps | ✅ backend error tracking + uptime monitoring both live; frontend error tracking deliberately deferred (founder's explicit choice). Backend: `io.sentry:sentry-spring-boot-4:8.56.0`, gated the same way as every other third-party integration in this project (blank `SENTRY_DSN` = fully inert, verified via the full test suite booting the real Spring context with zero Sentry activity). Founder created a free Sentry project and supplied a real DSN; set as `SENTRY_DSN`/`SENTRY_ENVIRONMENT=production` on Render via the Render API, redeployed, verified live (`/actuator/health` → `{"status":"UP"}`) — actual event delivery isn't independently confirmable without a Sentry API token, so that's left for the founder to eyeball in their Sentry dashboard once a real error occurs. `send-default-pii: false` by default (privacy). **Frontend Sentry attempted and reverted, then deliberately left backend-only:** `@sentry/nextjs` broke the local Windows build — importing it anywhere (even client-only) makes Next's build tracer pull in `import-in-the-middle`, which needs a symlink Windows blocks without Developer Mode enabled (confirmed not enabled on this machine, no admin rights available to enable it). Reproduced twice, isolated to exactly that cause, fully reverted (confirmed Workers bundle back to exact baseline size). Founder chose to leave it backend-only for now rather than enable Developer Mode or move to a Linux CI deploy pipeline — both remain available as future options if frontend coverage is wanted later. **Uptime monitoring:** founder set up UptimeRobot independently (self-contained, no integration needed on this end) — `GET /actuator/health` on the backend and the frontend root URL are the two live monitor targets. |
| 25 | Documentation set (README, ARCHITECTURE, DATABASE, DEPLOYMENT, MONETIZATION, SECURITY, ADMIN_GUIDE, GOOGLE_SERVICES_SETUP, ENV_VARS, API docs via Swagger UI) | Ongoing | Low | All docs present and accurate at launch | ✅ done. Root `README.md` rewritten (was badly stale — still referenced Phase 4 and claimed Node.js wasn't installed). All 7 new reference docs added under `docs/`: `ARCHITECTURE.md`, `DATABASE.md`, `DEPLOYMENT.md`, `SECURITY.md`, `MONETIZATION.md`, `ADMIN_GUIDE.md`, `GOOGLE_SERVICES_SETUP.md`, `ENV_VARS.md` — written from the real, live system (verified against actual migration files and package structure, not from the original planning-phase design doc, which had drifted: no `AdminUser`/role entity or `Subscription` table was ever actually built). API docs already covered by existing `docs/api/README.md` (springdoc-openapi, no separate static doc needed). `docs/README.md` index updated to link everything, split into "reference" (how it works now) vs. "planning/decision" (history) sections. |
| 26 | AI generation provider abstraction (backend) — see `decisions/07-ai-generation-initiative.md` | Phase 20's `PaymentProvider` pattern | Medium | Plumbing in place, inert until a key exists | ✅ built, tested, deployed inert. `com.extremis.hub.ai`: `AiGenerationProvider` interface (single-shot prompt→text, throws `AiGenerationException` on any failure/refusal/empty output so callers always fall back), `ClaudeGenerationProvider` (official Anthropic Java SDK 2.34.0; default model `claude-opus-5`, effort `low`, per-call `maxOutputTokens` ceiling, 45s timeout, 1 retry; thinking left at model default rather than disabled — disabling has known failure modes on Opus 5), `AiProviderConfig` (same null-bean gate as PhonePe: no bean unless `ANTHROPIC_API_KEY` is set AND `AI_GENERATION_ENABLED` isn't false), and an admin-only `POST /api/v1/admin/ai/ping` smoke test (one tiny call) to prove a new key works end-to-end without touching any customer tool. 4 unit tests on the gating logic; full suite green incl. the real Spring context booting with the SDK on the classpath. **Honestly unverified: the live API call itself** — no `ANTHROPIC_API_KEY` exists yet (founder action: Anthropic Console account with billing → business API key). Model choice is a founder cost decision: default is the flagship (`claude-opus-5`, $5/$25 per MTok); `claude-sonnet-5` ($2/$10) is a one-env-var switch (`ANTHROPIC_MODEL`), not a silent downgrade. |
| 26b | Generic OpenAI-compatible AI provider (FreeModel first; Cloudflare Workers AI / OpenRouter by config only), `AI_PROVIDER` switch, lenient JSON handling | Phase 26 | Medium | Live call verified with a real key | ✅ built + tested; live-verified against the gateway from a script. `OpenAiCompatibleGenerationProvider` (plain `RestClient`, base URL/key/model all config, no defaults), `AI_PROVIDER` switch in `AiProviderConfig` (openai-compatible default, `anthropic` explicit), `servedBy` (the upstream model that actually answered) on every result. 13 tests, 7 of them real HTTP round trips against an in-process fake server incl. the failure shapes seen live. **Measured on the real FreeModel free tier (2026-09-22):** the console's API host is `alli.website` (a bare IP, not `freemodel.online`, which is behind Cloudflare and doesn't list the `fm-v1-*` ids); `fm-v1-lite` works (~2-4s) and is served by a small free upstream (`inclusionai/ling-3.0-flash-sante:free`, OpenRouter-style response) — **not Claude**; `fm-v1-standard` / `fm-v1-pro` return HTTP 402 (paid points). It is a reasoning model: at `max_tokens=900` it returned EMPTY content (`finish_reason=length`) 2 runs in 3; at 3000 it returned valid JSON 5/5 — so the provider floors the budget at 3000 and treats empty content as a failure. Output quality is a template-comparable baseline (fine but formulaic), not frontier. **Update (same day, after reading the guide):** the founder relayed the full `/guide` page content — it's a technical quick-start config generator (Claude Code MCP, Codex CLI, Cursor, Continue.dev, Cline, cURL, Python, Node.js snippets), not a terms/policy page. Its own recommended defaults (`freemodel.online` + `auto/best-chat`) were tested live and don't currently work (502/400s) — confirmed `freemodel.online` and `alli.website` are two different backends with two different model catalogs under the same key; `alli.website` + `fm-v1-lite` remains the one combination that has ever actually worked. **Commercial-use/data-retention terms remain genuinely unread — no policy page has been found for this product yet**, not merely an unread one; still open for Phase 27. |
| 26c | Free.ai fallback slot + round-robin/failover between two AI backends, Privacy Policy disclosure | Phase 26b | Medium | Live-verified with real requests distributed and a real failure surviving | ✅ built, tested, deployed, **and live-verified in production with both real keys.** `OpenAiCompatibleProperties.baseUrl` renamed to `chatUrl` (breaking: `AI_API_BASE_URL` → `AI_API_CHAT_URL`) after finding two real, different URL conventions (FreeModel: `{base}/chat/completions`; Free.ai: `{base}/v1/chat/`, no "completions", trailing slash) — a single suffix-appending base URL couldn't express both. `AiCompatPropertiesConfig` binds `OpenAiCompatibleProperties` at two prefixes (`extremis.ai.compat`/`compat2`) as its own `@Configuration` class — doing this inside `AiProviderConfig` itself would be circular (Spring must construct that class via its constructor before calling any of its own `@Bean` methods), caught before it reached a running context. New `CompositeAiGenerationProvider`: round-robins starting at the next delegate each call (not "primary until it dies"), fails over to the other delegate on any error before surfacing a failure. Neither slot configured → inert (unchanged); one configured → that provider directly; both → wrapped in the composite. 12 new tests (5 for the composite: even distribution over 10 calls, fail-over, all-failed) — 76 backend tests total, 0 failures, incl. the real Spring context booting with both compat-properties beans present. **Also fixed a real live bug found mid-task:** the founder's key round-tripped against the wrong domain at first — the console's own quick-start snippet uses `alli.website`, not `freemodel.online` researched in Phase 26b (different DNS, a smaller public model list); Render's `AI_API_CHAT_URL` corrected to the real, exact endpoint (`https://alli.website/v1/chat/completions`) and re-verified live via the admin ping. **Privacy Policy** gained a positively-framed "Smarter results from AI-assisted tools" section (Title Generator, Description Generator, and the planned-but-not-yet-built AI Thumbnail Generator, careful not to claim it exists) — discloses third-party AI processing and the always-available template fallback without naming a specific vendor, since the active backend is config-driven. **Update (same day, second real key supplied):** Free.ai live-verified — `qwen7b` returned valid JSON 5/5 real title-generation calls (~5-6s each). Both slots configured on Render and round-robin re-verified live in production: 6 consecutive `/api/v1/admin/ai/ping` calls alternated perfectly, `inclusionai/ling-3.0-flash-sante:free` (FreeModel) ↔ `Qwen/Qwen3-30B-A3B-Instruct` (Free.ai), three full cycles, 404ms-2006ms each. `/api/v1/game-deals` unaffected. See `decisions/07-ai-generation-initiative.md` §10 for full detail. |
| 27 | Title & Description Generator AI upgrade, premium-gated | Phase 26, Phase 22 | Medium | Premium users get real AI-generated output; free users see the unchanged deterministic templates; any AI failure falls back silently | ✅ built and tested locally (91 backend tests, 0 failures, incl. the real Spring context against the live Neon DB). `TitleGeneratorService`/`DescriptionGeneratorService` each gained an overloaded `generate(request, useAi)` — the existing single-arg `generate(request)` is unchanged and still template-only, so nothing about the free path moved. `useAi` is decided in the controller, not the service: `hasPremiumAccess(userId)` (the same Phase 22 entitlement, `"premium-tools"`) — a finer-grained check than the tool-level `requireAccessIfPremium` gate already there, so a premium user gets AI generation even on a tool that isn't itself marked `premiumOnly`. Both services build a structured-JSON prompt matching their own response shape, call `Optional<AiGenerationProvider>`, parse with Jackson, and validate the result before returning it: title/short-title length caps (100/40 chars), hashtag shape (`#`, no spaces, capped at 15), description length cap (5000 chars, YouTube's real limit) — and a new shared `BannedAbsoluteClaims` runtime filter (extracted from what was previously test-only logic in `TitleGeneratorServiceTest`, now the single source of truth for both the test and the runtime check) rejects any unverifiable absolute claim. Title validation is per-title (one bad title among several doesn't discard the good ones) but rejects the whole AI result if fewer than 3 clean long-form titles survive. On no provider configured, a thrown `AiGenerationException`, malformed JSON, or failed validation, both services fall back to their existing template method — never an error surfaced to the customer. 15 new tests across two new `*ServiceAiTest` classes (valid JSON, provider failure, malformed JSON, banned-claim filtering both partial and total, code-fence stripping, no-provider-configured, `useAi=false` never calls the provider). **Live-verified end-to-end in production, driving the actual deployed UI (not a direct API call) as the founder's own already-premium account** — and that live verification caught a real, separate, pre-existing production bug in the process: `postJson` (`frontend/lib/api.ts`), the fetch helper behind every `/api/v1/tools/*` call including both generators, **never attached an `Authorization` header at all**. Consequence: `Authentication` was always `null` server-side for these endpoints, so (a) Phase 27's `useAi` always evaluated `false` regardless of entitlement, and (b) Phase 22's existing premiumOnly gate on `gaming-description-generator` was unreachable from the real website — a signed-in premium user submitting the actual form got a 401 "not authenticated," not the intended result (Phase 22's own verification tested the gate via a direct API call with a manually-attached token, which never exercised this path). Fixed by making `postJson` attach `Authorization: Bearer <token>` whenever `lib/auth.ts`'s `getToken()` has one, and omit it otherwise — every tool stays fully usable anonymously, signed-in callers just become visible to the backend. Re-verified live after the fix and the redeploy: both tools now return genuine AI-generated output for the founder's premium account (natural, varied prose with emojis — unmistakably not the deterministic templates, e.g. "Hey squad! 🎮 Welcome back to EXTREMIS Plays!..." and "I DEFUSED THE ENTIRE ROUND ALONE! 🔥 INSANE 1v5 ACE CLUTCH ON BIND IN VALORANT"), with correct validation (15 hashtags capped, no banned claims). Also corrected two tool FAQ/meta copy spots that flatly claimed "no AI" — true for free users, no longer true for premium ones. |
| 28 | AI cost & abuse controls (rate limiting, daily spend ceiling, generation log, admin visibility) | Phase 27 | Medium | Per-user rate limit and a global daily ceiling both actually stop calls before they reach the provider; every attempt (success/failure/throttled) is logged; admin panel shows real usage | ✅ built and tested (108 backend tests, 0 failures, incl. the real Spring context booting the new V10 migration against the live Neon DB). New `AiUsageGuard` (`com.extremis.hub.ai`) is the circuit breaker: an in-memory per-user rolling-60s rate limit (`AI_RATE_LIMIT_PER_MINUTE`, default 5) and a global daily call ceiling (`AI_DAILY_CALL_CEILING`, default 200) -- **calls, not dollars**, since neither FreeModel nor Free.ai publish per-call pricing (see `07-ai-generation-initiative.md` §8-10), so a call count is the honest, measurable proxy actually available; this is the product's equivalent of Triage Desk's `--max-budget-usd`. A single Render instance, so plain `ConcurrentHashMap`/`AtomicInteger` counters are enough -- no Redis needed. `TitleGeneratorService`/`DescriptionGeneratorService` both call `usageGuard.tryAcquire(userId)` *before* ever reaching the AI provider; a denial falls straight back to templates without spending a call. New `ai_generation_log` table (V10 migration) -- one row per attempt (success, failure, or throttled), metadata only (provider, model, servedBy, tokens, latency, outcome), **never the prompt or response text**, matching `AiGenerationException`'s own no-user-content rule. New admin endpoint `GET /api/v1/admin/ai/usage-today` (`AiUsageService` aggregates today's UTC rows: totals, success/failure/throttled split, token sums, average latency, per-provider breakdown) and a new "AI usage today" card in the admin panel (`AdminAiUsageCard.tsx`) with a visual ceiling progress bar that warns at 80%. 13 new backend tests (`AiUsageGuardTest`: per-user vs. global limiting, zero-ceiling edge case, skip-logging-gracefully when a user no longer exists; `AiUsageServiceTest`: bucket splitting, token/latency aggregation, empty-day handling) plus 4 new integration-level tests in the existing `*ServiceAiTest` classes proving a throttled call never reaches the provider. **Verified live end-to-end in production:** deployed (Render + Cloudflare), then drove a real title generation through the actual UI as the founder's premium account. The admin "AI usage today" card correctly went from all-zero to `Calls today: 1, Succeeded: 1, Input tokens: 148, Output tokens: 2457, Avg latency: 13417ms, round-robin: 1` -- confirming the full pipeline (`tryAcquire` → real AI call → `recordSuccess` → persisted `ai_generation_log` row → `AiUsageService` aggregation → the admin card) all work correctly against real production data, not just the unit tests' simulated guard. |
| 29 | Interactive refine chat (optional, later) | Phase 27 | Medium-High | After an AI generation, a signed-in premium user can say "make it punchier" and get a revised result in the same conversational context; closes automatically on inactivity or a turn cap | ✅ built and tested (130 backend tests, 0 failures, incl. the real Spring context against the live Neon DB). `AiGenerationRequest` gained an optional `history` field (a new 4-arg canonical constructor; every existing 3-arg call site is untouched and still gets empty history) carrying `ConversationTurn` records through to both providers -- `OpenAiCompatibleGenerationProvider` threads them into the `messages` array between the system and final user message; `ClaudeGenerationProvider` chains the SDK's `addUserMessage`/`addAssistantMessage` in order (confirmed via `javap` against the real `anthropic-java-core` jar rather than guessed -- the public `anthropic-java` artifact is an empty aggregator; the real classes live in `anthropic-java-core`). New `RefineSession`/`RefineSessionStore` (`com.extremis.hub.ai`): deliberately NOT persisted -- a single Render instance, so an in-memory `ConcurrentHashMap` is enough, closed by a 10-minute inactivity TTL or a 6-turn cap (`RefineSessionStore.SESSION_TTL`/`MAX_REFINE_TURNS`), swept lazily on access. `TitleGeneratorService`/`DescriptionGeneratorService` each gained a `refine(sessionId, message, userId)` alongside `generate()`: on a successful AI generation, `generate()` now seeds a session with the system prompt + the raw AI JSON as the first assistant turn and returns its id as a new `refineSessionId` field (null for template output, which has no conversation to refine); `refine()` validates session ownership + tool-type match + the turn cap, reuses the same Phase 28 `AiUsageGuard` rate limit/daily ceiling and `ai_generation_log` write, and -- unlike `generate()` -- has **no template fallback**: any problem (expired/unknown/wrong-owner session, turn cap reached, no provider, a failed call) throws a `BusinessRuleViolationException` (422) instead of silently degrading, since there's nothing to "refine" about a template. New `POST .../gaming-title-generator/refine` and `.../gaming-description-generator/refine` endpoints, gated on the same premium entitlement as the AI generation path (a free/signed-out caller never had a `refineSessionId` to refine in the first place). Frontend: a shared `RefineBox` component renders under a result only when `refineSessionId` is present, posts the instruction, and swaps in the revised result. 17 new backend tests (`RefineSessionStoreTest`: ownership/tool-type isolation, TTL boundary; refine-flow tests folded into the existing `*ServiceAiTest` classes: valid refine keeps the session id, unknown/malformed/wrong-owner session, provider failure, turn-cap enforcement, no-provider-configured -- all asserting the thrown exception rather than a template fallback) plus one new `OpenAiCompatibleGenerationProviderTest` case proving history turns are sent in the correct order. **Verified live end-to-end in production:** deployed (Render + Cloudflare), generated real titles for "Apex Legends / Ranked grind to Predator" as the founder's premium account, then typed "make these way funnier and shorter" into the new refine box. The AI genuinely used the conversation history -- the revised titles ("Skill Issue", "RNG Prayers", "Apex Anxiety", "Predator Grind: Me Thinking I'm Good at Apex") were a completely different, deliberately funnier and shorter tone from the originals, not a random regeneration. The admin usage card correctly counted both calls (`Calls today: 3` including one from an earlier verification session, all succeeded, `round-robin: 3`), confirming refine() shares Phase 28's rate limit/logging correctly. |
| 30 | Custom domain cutover: DNS to Cloudflare (`creator-hub.co.in`, founder-purchased GoDaddy) — see `decisions/08-custom-domain-cutover.md` | Domain purchased (done, 2026-09-22) | Low | Cloudflare zone shows "Active"; nameservers resolve to Cloudflare | ✅ done by the founder — confirmed live via the Cloudflare API: zone `creator-hub.co.in` status `active`, nameservers `kara.ns.cloudflare.com`/`wells.ns.cloudflare.com`, original registrar recorded as GoDaddy. |
| 31 | Custom domain cutover: frontend (Cloudflare Workers custom domain) | Phase 30 | Low | `https://creator-hub.co.in` serves the live site over a valid cert; `www` redirects to it | ✅ done and live-verified, both hostnames. `wrangler.jsonc` bound `creator-hub.co.in` as a Worker custom domain; `https://creator-hub.co.in` returns 200 with real SSR-fetched Hot Game Deals content over a valid cert. **Caught before shipping:** adding a `routes` array silently disables the `workers.dev` route by default in Wrangler — added `"workers_dev": true` explicitly so the old URL keeps working, matching the plan's own "additive, not a hard cutover" rule. `www.creator-hub.co.in` bound once the founder removed a pre-existing conflicting DNS record in the zone (likely carried over from GoDaddy's default parking setup); `curl -I` confirms a clean `301` to the bare domain. |
| 32 | Custom domain cutover: backend (Render custom domain, `api.creator-hub.co.in`) | Phase 30 | Low-Medium | `https://api.creator-hub.co.in/actuator/health` returns UP with a valid cert | ✅ done by the founder, confirmed live. Render shows the custom domain `verificationStatus: verified`; `https://api.creator-hub.co.in/actuator/health` returns real `{"groups":["liveness","readiness"],"status":"UP"}` over a valid cert. |
| 33 | Custom domain cutover: application config (env vars, CORS, redeploy, live verification) | Phases 31-32, founder's Google OAuth origin update | Medium | Every feature verified working on the new domain with real data, not just status codes | ✅ done and verified live. `frontend/.env.production.local` updated (`NEXT_PUBLIC_API_BASE_URL=https://api.creator-hub.co.in`, `NEXT_PUBLIC_SITE_URL=https://creator-hub.co.in`) and rebuilt+redeployed — confirmed the new bundle contains the new hostname 3× and zero occurrences of the old `onrender.com` one. Backend `extremis.cors.allowed-origins` gained the new frontend origin (old `.workers.dev` origin kept as a transitional fallback) and redeployed; 130 backend tests still green. Live-verified: `https://creator-hub.co.in` serves real backend-fetched deals content (not just a 200); `/login` renders the real Google sign-in button with no origin-mismatch console error and "Backend: connected" in the footer — the Google OAuth origin appears to already be configured correctly (not independently confirmed by completing an actual sign-in, which this assistant doesn't do). |
| 34 | Custom domain cutover: SEO/doc polish (optional) | Phase 33 | Low | Docs and sitemap/canonical URLs reference the new domain | ✅ done and verified live. `README.md`/`DEPLOYMENT.md`/`GOOGLE_SERVICES_SETUP.md` updated to name `creator-hub.co.in`/`api.creator-hub.co.in` as the canonical URLs, with the underlying `*.workers.dev`/`*.onrender.com` addresses kept (not deleted) as the documented infrastructure fallback -- including fixing `DEPLOYMENT.md`'s `.env.production.local` recreate-instructions, which still showed the old URLs and would have silently regressed a fresh setup. `GOOGLE_SERVICES_SETUP.md`'s Search Console section updated to the cleaner Domain-property + DNS-TXT verification path, natural now that DNS is on Cloudflare. Live-checked (not assumed) the actual deployed HTML/files: `sitemap.xml` and `robots.txt` both correctly use the new domain and contain zero references to the old one; `<link rel="canonical">`, all `og:*`, and `twitter:*` tags all correctly resolve to `https://creator-hub.co.in`. **Incidental finding, not a bug:** moving the zone to Cloudflare added an automatic "Content-Signal" block to `robots.txt` (Cloudflare's own bot-management feature, not this app's code) disallowing a long list of AI-training crawlers by default -- a reasonable default, left as-is, noted here so it isn't mistaken for unexplained behavior later. No redirect from the old `.workers.dev` URL was added, per the original plan -- both remain live indefinitely at zero cost. |
| 35 | AI model optimization: eval harness for Title/Description generation — see `decisions/09-ai-model-optimization.md` | Phase 27 | Medium | A runnable eval with a measured baseline score for the current prompts | ✅ done and live-verified. 24 real cases (12 Title, 12 Description, every Tone×VideoType pair plus edge cases) run against the real production system prompts and the real `alli.website` gateway — `backend/.claude/hillclimb/ai-title-description/` (`run-eval.mjs`, `cases.json`, `report.html`). Grading, per the founder's explicit choice (no Claude API key configured): a deterministic `valid`/`distinct_from_template` pass (100% on both, 24/24) plus a `quality` (0-3) pass grounded in real YouTube search results rather than an abstract rubric — see `QUALITY_PASS.md` in that folder. **Real baseline findings, not assumed:** descriptions score 3/3 across all 12 (natural, correctly weaves channel name/social links, genre-appropriate hashtags); titles score 2/3 on 11/12 (correct and on-topic, but the 5-8 options per case are near-permutations of the same words — real comparable titles vary the angle, e.g. numbered lists or parenthetical qualifiers, which the prompt never asks for); one edge case (10 keywords supplied) scores 1/3 — the model crammed every keyword into ALL CAPS, reading as spam. These three findings are the concrete input to Phase 36. |
| 36 | AI model optimization: prompt/few-shot iteration against the eval | Phase 35 | Medium | Measurably improved eval scores from few-shot examples and tighter prompt structure, no new endpoints | ✅ done and live-verified. `backend/.claude/hillclimb/ai-title-description/` v1→v2, via `claude-api`'s `hillclimb` flow. **v1** (asked for a varied opening hook per title + capped keyword usage at 3-4/title): quality 1.958→3.0/3 (ceiling) on successful attempts, but regressed reliability — 3/24 attempts (12.5%) hit `finish_reason=length` (hidden reasoning ate the whole token budget on the more detailed prompt). **v2** (same prompt, `AI_MAX_OUTPUT_TOKENS` 3000→4500 — a provider-parameter fix, not a prompt rewrite): eliminated all 3 failures, held quality at 3.0/3 ceiling, both guardrails (`valid`, `distinct_from_template`) at 100%. Shipped to `TitleGeneratorService.java`; all 130 backend tests green. No new endpoints, exactly as planned. |
| 37 | AI model optimization: first-party usage-signal capture (copy/refine/regenerate) | Phase 28 | Medium | `ai_generation_log` (or a companion table) records a lightweight outcome signal per generation | ✅ done. New `outcome_signal` column (V11 migration), `OutcomeSignal` enum (COPIED/REFINED/REGENERATED). REFINED and REGENERATED are inferred entirely server-side (RefineSession now carries the originating log row's id; refine() marks it REFINED, a new generate() call checks for and marks a still-live unsignaled prior session REGENERATED) — zero new UI. COPIED needed one small, invisible hook: the existing Copy buttons gained an optional `onCopy` callback that fire-and-forgets `POST .../copied` when a premium AI result (one with a `refineSessionId`) is copied; template results have no session, so free users are untouched. Shared `GenerationSignalService` (first-signal-wins for the inferred two; COPIED always allowed to overwrite). 146 backend tests (16 new), `tsc` clean. |
| 38 *(optional)* | AI model optimization: admin visibility into eval scores + usage signals | Phase 36, 37 | Low | Eval score trend and usage-signal breakdown visible in the admin panel | — proposed, not started |
| 39 *(optional, founder budget decision)* | AI model optimization: real fine-tuning evaluation | Phase 35-37 | High | Only revisited if prompt iteration plateaus and the founder chooses to spend real money — see doc 09 §6 for real cost figures | — proposed, not started |
| 40 | AI Thumbnail Generator: image provider + live verification — see `decisions/10-ai-thumbnail-generator.md` | Phase 26's provider pattern | Medium | Confirms base64 reference-image support, maps the free-tier ceiling, verified at real 1280×720 output | — proposed, not started |
| 41 | AI Thumbnail Generator: backend tool service + endpoint | Phase 40 | Medium | `POST /api/v1/tools/gaming-thumbnail-generator`, premium-gated, reuses Phase 28's `AiUsageGuard` under a new `ToolType.THUMBNAIL_GENERATOR` | — proposed, not started |
| 42 | AI Thumbnail Generator: SEO/description-aware prompt construction | Phase 41, Phase 27 | Medium | Thumbnail generation shares game/topic/keyword context with the Description Generator for topical/visual consistency | — proposed, not started |
| 43 *(optional)* | AI Thumbnail Generator: ephemeral YouTube style-reference lookup | Phase 42, founder YouTube Data API key | Medium | Per-request-only style inspiration, never cached past use, explicitly gated on staying inside YouTube's Developer Policies (see doc 10 §1) — skipped entirely if it can't be done cleanly | — proposed, not started |
| 44 | AI Thumbnail Generator: frontend UI + "Coming Soon" flag flip | Phase 41 | Medium | New tool page live; homepage's existing "Coming Soon" card becomes a real link; Privacy Policy's "planned, not yet built" language updated | — proposed, not started |
| — | Post-domain-cutover fixes: sign-in redirect/stale header bug, `www` binding, favicon (founder-reported, not in original roadmap) | Phase 33 | Low | Sign-in lands on the home page with the header correctly showing the signed-in avatar; `www.creator-hub.co.in` resolves; the site has a real favicon | ✅ verified live. **Real sign-in test on the new domain caught two bugs:** (1) landed on `/dashboard` instead of the home page after signing in; (2) the header kept showing "Sign in" even though the page body correctly showed the signed-in account (screenshot evidence: dashboard content signed-in, header above it not). **Root cause:** `GoogleSignInButton` used `router.push("/dashboard")` -- a client-side route change. `AuthNav` (in the persistent layout) only checks the stored token once on mount and never unmounts across a client-side navigation, so it stayed stuck showing whatever it decided on the very first page load, before sign-in. Fixed by using a real navigation (`window.location.href = "/"`) instead: lands on the home page as expected, and forces the header to remount and re-check the now-valid token. **`www.creator-hub.co.in`:** re-added to `wrangler.jsonc`'s custom domain routes once the founder deleted the pre-existing conflicting DNS record; new `frontend/proxy.ts` (see the next row for why not `middleware.ts`) redirects it to the bare domain (308) as a backstop, though live testing shows the actual 301 is coming from Cloudflare's own custom-domain layer before the Worker runs -- confirmed live via `curl -I`. **Favicon:** the site had none. New `frontend/app/icon.svg` -- a bold "E" monogram in the site's own brand-primary red (`#ff3b3b`) on its near-black `bg-canvas` (`#0a0b0f`), Next.js's auto-detected icon convention, no further wiring needed. Checked legible down to 16×16 before shipping; confirmed live in the deployed page's `<head>` (`<link rel="icon" href="/icon.svg" .../>`). |
| — | Favicon robustness + branded route-transition loader (founder-reported/requested, not in original roadmap) | Phase 33 | Low-Medium | Founder's browser still showed a generic icon; a transparent animated "CREATOR HUB" splash appears between page navigations | ✅ verified live. **Favicon still not showing in the founder's tab** despite the server-side `<link>`/file both being confirmed correct -- almost certainly that specific browser tab's sticky favicon cache (opened before the icon was deployed), not a server issue. Added `app/icon.png` and `app/apple-icon.png` as raster fallbacks alongside the existing `icon.svg` regardless, for any client that renders SVG favicons inconsistently and for iOS home-screen bookmarking -- confirmed live: all three `<link>` tags present in `<head>`, all three files serve with correct content-types. **Also caught mid-task:** `middleware.ts` is deprecated in Next.js 16.0 in favor of `proxy.ts` (confirmed via the bundled `node_modules/next/dist/docs`, not guessed, per this project's own `AGENTS.md` warning about this Next.js version's API drift) -- renamed before it ever shipped; same behavior, new file/export name. New `components/RouteLoadingOverlay.tsx`: a transparent, blurred splash between page navigations with "CREATOR HUB" animating in letter-by-letter (staggered pop-in + a red glow-pulse, keyframes in `globals.css`), mounted once in the root layout. No single built-in App Router hook exists for "a client-side navigation just started" across Next.js versions, so it listens for clicks on internal links directly -- deliberately *not* checking `event.defaultPrevented`, since `next/link` always calls that on the native click as part of intercepting browser navigation, so checking it would skip every real Link click (a real bug caught during testing, not a guess) -- and clears itself once the pathname actually changes, holding a minimum 550ms so a fast navigation still animates visibly, with a 4s safety-net timeout. **Verified via `MutationObserver` (ground truth, not polling, since a naive polling-based test script produced flaky/misleading results)** in both local dev and production: the overlay was added ~2-4ms after a real click and removed roughly 970-1045ms later, once navigation completed -- correct end-to-end on both the minimum-hold guarantee and cleanup. **One real caveat surfaced by the deploy log, not silently ignored:** OpenNext's Cloudflare adapter reports Node.js middleware/proxy support as "experimental... not officially maintained by OpenNext maintainers" -- functioning correctly as of this deploy (re-verified live afterward) but worth remembering if a future Cloudflare or OpenNext update ever changes this specific behavior. |

| — | Pricing page, nav/header restructure, multi-store deals, creator-gear research (founder-requested, not in original roadmap) | Phases 18-22 | Medium | Real pricing page live; nav reorganized; game deals cover Steam+Epic at a real quality bar; gear-deals feasibility researched | ✅ verified live. **Pricing page** (`/pricing`, new): three real tiers -- Free (live tool list), Digital Products (live prices fetched from `/api/v1/products`, "Coming soon" when none active rather than a fabricated price), Premium (honestly framed as invite-only, no self-serve checkout exists yet, links to Contact) -- no invented numbers anywhere. **Header**: `AdminNavLink`/separate admin+name+sign-out links replaced by a single circular avatar button (`AuthNav.tsx`) -- shows the real Google profile picture once signed in (new: `GoogleTokenVerifierService` now extracts the standard OIDC `picture` claim, stored on `Profile.avatarUrl`, returned as `CurrentUserResponse.avatarUrl`; refreshed on every sign-in so it stays current with the user's actual Google avatar), falls back to a letter-avatar circle when no picture, opens an animated dropdown (Dashboard/Admin/Sign out); "Sign in" unchanged when signed out. Nav order changed to Tools → Store → Deals → Pricing; the standalone Gear nav link was dropped -- `/deals` now shows Hot Game Deals (left) and Recommended Gear (right) side by side on one page (the old `/gear` URL still works standalone, just no longer linked from the header). Homepage hero restructured to a 2-column layout (hero text left, Hot Game Deals right) on desktop, stacking on mobile. **Hot Game Deals quality fix**: founder correctly flagged that results included 95%-off shovelware with 0% positive Steam ratings. Root cause was zero quality filtering on real, already-available CheapShark rating data (`steamRatingPercent`/`steamRatingCount`) -- fixed by requiring ≥70% positive from ≥20 reviews. Also broadened from Steam-only to Steam + Epic Games Store (storeID 25, confirmed live against CheapShark's own `/stores` endpoint, not guessed), and switched thumbnails from CheapShark's small 231×87 capsule to Steam's own CDN `header.jpg` (460×215, confirmed loading, real file size ~44KB) for every deal -- verified live: 14 deals now returned (was showing junk before), mixed Steam/Epic, all ≥70% rated, all with sharp thumbnails. Non-Steam deals correctly use CheapShark's own official `/redirect?dealID=` endpoint (Epic doesn't map from a Steam app ID) rather than a fabricated URL. **Creator-gear deals research (honest finding, no viable free/open source):** researched Best Buy API (requires a keyed partner relationship), Newegg Marketplace API (seller-only, key issued by email, not a public deals feed), Slickdeals (RSS is technically public but their own Terms of Service explicitly prohibit commercial use without a partnership -- direct conflict with this monetized site, and a direct WebFetch attempt was itself blocked with 403), and camelcamelcamel (ToS explicitly prohibits both scraping and republishing data, specifically calling out affiliate-site reuse -- exactly this use case). Conclusion: same real constraint already documented for Phase 18 general affiliate links -- there is no legitimate free/open API for real-time creator-gear deals without either enrolling in an affiliate program (which then unlocks a real API, e.g. Amazon's Product Advertising API via Associates) or paying for a commercial price-comparison service. Did not build a scraper against a site whose own terms prohibit it. Interim real improvement instead: `AffiliateProduct` gained an `imageUrl` field (V9 migration) end-to-end (entity → admin CRUD form → public card) -- affiliate gear cards had no image at all before this; now the founder can attach a real product photo per item, closing the visual-quality gap even without an auto-fetched feed. |

| — | Homepage hero teaser: rotating showcase replacing the static "Hot Game Deals" panel (founder-requested, not in original roadmap) | Phases 19, 21, 26c, 27 | Medium | The hero's right-column panel cycles through every non-empty section (All Tools → Premium Tools → Coming Soon → Hot Game Deals → Recommended Gear → Our Store) every 3s in a continuous loop | ✅ built and QA'd locally against the real backend (real Neon data, not mocks). New `HomeShowcase.tsx`: a fixed-height panel with a rotating conic-gradient glow ring (red↔accent), a per-slide progress-fill bar, clickable position dots, and a real overlapping crossfade (outgoing slide fades/slides out while the incoming one fades/slides in, not a plain swap). Each of the 6 sections is built server-side in `app/page.tsx` from real data (`lib/homeShowcaseSlides.tsx`) and skipped entirely when empty (e.g. no products in the store yet) rather than showing a blank slide -- confirmed live: with 0 store products, exactly 5 of 6 slides render. Pauses auto-advance (manual dot navigation stays available) on hover, on keyboard focus, when the tab isn't visible, and under `prefers-reduced-motion` -- confirmed all four independently, including one where the automated test browser's own OS-level reduced-motion setting made this immediately obvious rather than a needed simulation. **Two real bugs caught and fixed before shipping:** (1) the glow ring was originally a whole rotating rounded-rect div -- rotating a rectangle swings its corners outside its own box, visibly escaping the card; fixed by keeping the ring's outer box fixed+clipped and rotating an oversized gradient square inside it instead. (2) `tabHidden` state only updated on a `visibilitychange` event, never reading the real value at mount -- a tab opened already in the background would incorrectly start unpaused until the first visibility change; fixed to read `document.hidden` once on mount too. Verified via direct DOM/state inspection (not just screenshots, which were themselves initially misread once due to timing) that dot index, heading text, and auto-advance all stay consistent across manual navigation, auto-advance, and the crossfade's overlap window. |
63	
64	| — | **Incident: production login/backend connectivity broken (2026-09-14, founder-reported)** | — | — | Login and all backend-dependent features working again in production | ✅ fixed and verified live. Founder reported login broken with a screenshot showing "Backend: unreachable" in the footer. **Root cause:** the two frontend deploys immediately prior (Pricing/avatar/deals work, and the About/Privacy/Terms/Contact pages before that) ran `npm run deploy` without overriding `NEXT_PUBLIC_API_BASE_URL`, so Next.js loaded `frontend/.env.local`'s local-dev value (`http://localhost:8080`) for the production build instead -- no `.env.production.local` existed to take precedence. This broke backend connectivity for every real visitor, both server-side (SSR fetches like Hot Game Deals silently returned empty) and client-side (Google sign-in POSTs to a URL that doesn't exist for the visitor's own browser). CI and `tsc` stayed green throughout since this is an env-var/runtime issue, invisible to either. **Process gap that let it ship:** my own post-deploy verification after those two deploys checked HTTP status codes and static markup only, never confirmed real backend-fetched data actually reached the client -- a 200 on a page proves it rendered, not that it has real data. **Fix:** created `frontend/.env.production.local` (gitignored, same as `.env.local`) with the real production `NEXT_PUBLIC_API_BASE_URL`/`NEXT_PUBLIC_SITE_URL`/`NEXT_PUBLIC_GOOGLE_CLIENT_ID`, rebuilt, redeployed. Verified live: homepage SSR now shows real Hot Game Deals content, `/tools` shows real premium badges, and the actual client JS bundle was scanned and confirmed to contain the real `onrender.com` backend hostname (and confirmed to contain zero occurrences of `localhost:8080`). `DEPLOYMENT.md` updated with this incident, the required `.env.production.local` contents, and a stronger redeploy checklist (check `next build`'s "Environments:" log line; verify real data, not just status codes). |

**Monetization phase ordering note:** phases 18-22 follow the founder-
approved sequence — Digital Products (19) before Affiliate (18 can run
in parallel since both are founder-controlled) before Display Ads (21)
before Premium/Subscription (22) — gated on real traffic validation
after phase 14-15 ship, not on a fixed calendar date.

**Explicit stop point:** per the founder's own instruction, implementation
(Phase 1 onward) does not begin until this blueprint set is approved.

**Deferred, not dropped — Neon Functions / Object Storage / Neon Auth:**
during Phase 5 setup, the Neon CLI's `skills`/`config` flow surfaced
Neon's separate serverless platform (edge Functions, S3-compatible
buckets, and a hosted "Neon Auth" service) as an available next step.
The founder decided to **hold this until last** and keep Spring Boot
as the single owner of business logic, auth, and data access for the
MVP, per the already-approved architecture (`02-technical-blueprint.md`).
Revisit only after the MVP tools are live and there's a concrete reason
(e.g. Neon Auth as a lighter-weight alternative to hand-rolled Spring
Security + Google OAuth, or Object Storage as an alternative to signed
Spring Boot download URLs for Phase 19's digital products) — not by
default just because the tooling offers it.
